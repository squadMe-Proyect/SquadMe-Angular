// Representa un objeto Media.
export interface Media {
    id: number;              // ID del medio.
    url_small: string;       // URL del medio en tamaño pequeño.
    url_medium: string;      // URL del medio en tamaño mediano.
    url_large: string;       // URL del medio en tamaño grande.
    url_thumbnail: string;   // URL de la miniatura del medio.
}
